# @package _global_
data:
  _name_: gauss-mix-model
  num_classes: 3 # possible y values
  num_xy_pairs_train: 32 # xy pairs including query
  num_xy_pairs_val: 32 # xy pairs including query
  dim: 16 # dimension of x and y
